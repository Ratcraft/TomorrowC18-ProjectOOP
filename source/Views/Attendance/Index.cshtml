@model Models.AttendanceCourseIdModel;

@{ ViewData["Title"] = "Index"; }

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>


<form asp-controller="Attendance" asp-action="Index" method="get">
    <p>

        <select asp-for="Id" asp-items="Model.Id">
            <option value="">All</option>
        </select>


        Title: <input type="text" name="searchString" />
        <input type="submit" value="Filter" />

    </p>


</form>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Attendances[0].id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attendances[0].courseId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attendances[0].facultyId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attendances[0].status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Attendances)
        {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.id)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.courseId)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.facultyId)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.status)
    </td>
    <td>
        <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
        <a asp-action="Details" asp-route-id="@item.id">Details</a> |
        <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
    </td>
</tr>}
    </tbody>
</table>
